#ifndef INPUT_H
#define INPUT_H

/**
* @brief Позволяет получить текст из файла по названию
* @param filename Название файла, из которого необходимо получить текста
* @return Возращает текст из файла
*/

char *read_file(const char *filename);

#endif  // INPUT_H
